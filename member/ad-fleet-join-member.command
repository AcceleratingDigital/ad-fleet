#!/bin/bash
# ad-fleet-join-member.command
# Double-click this file in Finder to join the AcceleratingDigital Fleet as a MEMBER machine.
# Opens Terminal and runs the installer automatically.

# Move to the directory containing this file
cd "$(dirname "$0")"

# Run the installer
bash ad-fleet-join-member.sh
